import { Center } from '@mantine/core';
import { MantineLogo } from '@mantine/ds';

const Home = () => {
    return (
        <Center my={150}>
            <MantineLogo size={100} />
        </Center>
    );
};

export default Home;
